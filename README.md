# Built a DAO with Thirdweb and Buildspace#

### **Welcome 👋**
To run the dao locally:

1. Run `npm install` at the root of your directory
2. Run `npm start` to start the project
3. Start coding!

